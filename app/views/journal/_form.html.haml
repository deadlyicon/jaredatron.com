= simple_form_for @journal_entry do |f|
  = f.error_notification

  .inputs
    = f.input :body, label: false

  .actions.pull-right
    = link_to 'cancel', @journal_entry.new_record? ? journal_entries_path : @journal_entry
    = f.button :submit, class: 'btn btn-primary'


